# Installation
## FrontEnd
1. Navigate to frontend folder through `cd frontend`
2. run ```npm install```, It will take some time
3. After that run ```npm start```

## Backend
1. Navigate to backend folder through `cd backend`
2. run `pip install -r requirements.txt`
3. After that `uvicorn main:app --reload`
